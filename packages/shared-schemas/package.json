{
  "name": "@repo/shared-schemas",
  "version": "3.0.0",
  "description": "Shared JSON schemas for Dixie Chemical Product Agent",
  "main": "./src/index.js",
  "scripts": {
    "build": "echo 'No build step required for JSON schemas'",
    "lint": "echo 'No linting required for JSON schemas'",
    "validate": "node scripts/validate-schemas.js",
    "clean": "echo 'No clean step required'"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^3.0.1"
  },
  "files": [
    "schemas/**/*.json",
    "src/**/*.js"
  ]
}
